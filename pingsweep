#!/bin/bash
# Wrapper script to run pingsweep from anywhere using the project's venv
SCRIPT_DIR="$(cd "$(dirname "$(readlink -f "$0" 2>/dev/null || realpath "$0" 2>/dev/null || echo "$0")")" && pwd)"
exec "$SCRIPT_DIR/.venv/bin/python3" "$SCRIPT_DIR/pingsweep.py" "$@"
